import Box from '@mui/material/Box';

const EditingBoard = () => {
  return <Box sx={{ width: '100%', height: '100%', p: 1 }}>Put content in EditingBoard component for the level</Box>;
};

export default EditingBoard;
